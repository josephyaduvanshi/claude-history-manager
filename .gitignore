# macOS
.DS_Store
.AppleDouble
.LSOverride

# Swift Package Manager
.build/
.swiftpm/
Packages/
*.xcodeproj/
xcuserdata/
DerivedData/

# Release artifacts (published to GitHub Releases instead of tracked)
dist/
*.app
*.dmg
*.pkg
Chronicle-*.zip

# Editor / IDE
.vscode/
.idea/
.cursor/
*.swp
*~

# Local Claude / agent state
.claude/
.superpowers/

# Internal planning docs (kept locally for reference, never published)
HANDOFF.md
.impeccable.md
context.md
docs/superpowers/

# Logs
*.log

# Sparkle update-signing private keys — never commit these.
# The matching public key lives in Chronicle/Resources/Info.plist.in
# and the private key lives in macOS Keychain locally + as the
# SPARKLE_ED_PRIVATE_KEY GitHub secret in CI.
sparkle-priv-key*
reddit-replies.md
